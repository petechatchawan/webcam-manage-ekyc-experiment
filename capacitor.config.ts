import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'webcam-manage-ekyc-experiment',
  webDir: 'www'
};

export default config;
